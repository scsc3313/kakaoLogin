<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kakao.sample.kakaotalk"
    android:versionCode="1"
    android:versionName="1.0.0">

    <uses-sdk android:minSdkVersion="10" android:targetSdkVersion="18"/>

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="@string/app_name"
        android:icon="@drawable/kakaotalk_sample_icon"
        android:theme="@android:style/Theme.NoTitleBar"
        android:name="com.kakao.template.loginbase.GlobalApplication">
        <activity
            android:name=".KakaoTalkLoginActivity"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".KakaoTalkSignupActivity" android:windowSoftInputMode="adjustResize"
                  android:launchMode="singleTask"/>

        <activity android:name=".KakaoTalkMainActivity" android:windowSoftInputMode="adjustResize"
                  android:launchMode="singleTop"/>

        <meta-data android:name="com.kakao.sdk.AppKey" android:value="@string/kakao_app_key"/>

    </application>

</manifest>